<form method="post" action="{{ path('app_news_post_delete', {'id': news_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_post.id) }}">
    <button class="btn">Delete</button>
</form>
